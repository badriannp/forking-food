import 'package:flutter/material.dart';

class AppColors {
  static const carrotYellow = Color(0xFFFFB300); // Primary
  static const darkGreen = Color(0xFF264E36);   // Secondary/accent
  static const background = Color(0xFFF9F9F9);  // Background principal
  static const surface = Color(0xFFF7F7F5);     // Carduri, sheet-uri etc.
  static const error = Colors.red;

  // Text & icon colors
  static const onPrimary = Color(0xFFF7F7F5);
  static const onSecondary = Color(0xFFF7F7F5);
  static const onBackground = Color(0xFF101010);
  static const onSurface = Color(0xFF101010);
  static const facebookBlue = Color(0xFF1877F2);

  // Accente suplimentare
  static const accent = Color(0xFFFFC94D);      // Galben mai deschis/accent
  static const disabled = Color(0xFFBDBDBD);    // Gri pentru stări inactive
  static const border = Color(0xFFE0E0E0);      // Gri deschis pentru borduri
}